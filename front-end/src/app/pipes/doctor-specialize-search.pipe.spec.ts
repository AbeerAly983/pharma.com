import { DoctorSpecializeSearchPipe } from './doctor-specialize-search.pipe';

describe('DoctorSpecializeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DoctorSpecializeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
