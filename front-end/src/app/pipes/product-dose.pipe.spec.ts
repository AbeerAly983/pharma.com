import { ProductDosePipe } from './product-dose.pipe';

describe('ProductDosePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductDosePipe();
    expect(pipe).toBeTruthy();
  });
});
