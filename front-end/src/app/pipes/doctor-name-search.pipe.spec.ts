import { DoctorNameSearchPipe } from './doctor-name-search.pipe';

describe('DoctorNameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DoctorNameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
