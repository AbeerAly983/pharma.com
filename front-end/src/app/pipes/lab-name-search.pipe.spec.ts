import { LabNameSearchPipe } from './lab-name-search.pipe';

describe('LabNameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LabNameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
